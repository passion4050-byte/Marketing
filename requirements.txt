# Streamlit Community Cloud 가 이 파일을 읽음 (pyproject.toml 무시).
# pyproject.toml 의 dependencies 와 동기화 유지.

streamlit>=1.30
sqlalchemy>=2.0
alembic>=1.13
pydantic>=2.5
python-dotenv>=1.0
pyyaml>=6.0
structlog>=24.1
httpx>=0.27
trafilatura>=1.12
beautifulsoup4>=4.12
chromadb>=0.5
apscheduler>=3.10
pymannkendall>=1.4

# LLM providers
google-generativeai>=0.7
anthropic>=0.40
openai>=1.50

# (선택) Postgres — Supabase 등 외부 DB 사용 시. SQLite로만 돌릴 거면 제거 가능.
psycopg2-binary>=2.9
