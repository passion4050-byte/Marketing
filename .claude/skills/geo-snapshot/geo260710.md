---
name: geo-snapshot-260710
description: 위서클(WECIRCLE·구 메디맵) GEO/AEO SaaS 작업 스냅샷 — 2026-07-10 Round 138. PMF 총력전 세션. ① 콘솔 홈 데모→실측 AI 인용 대시보드 교체(RPC dashboard_overview + /api/dashboard/overview, 커밋 1d0cc41) ② 데모 클린업 Phase 1(6페이지+5스텁 라우트 삭제·nav 정리, ee2df1e) ③ 출처 인용(source citation) 북극성 지표 추가 — "브랜드 언급 299 vs 우리 콘텐츠 출처 인용 0" 발견 ④ 콘텐츠 밀도 루틴 스케줄(wecircle-content-density, 월·수 주10개)+첫 배치 초안 5개(id198~202) ⑤ RLS 6개 테이블 잠금 ⑥ 학습/AB 파이프라인 정합(C=learned_pattern 성공신호 출처인용 결합, B=run_ab_analysis 슬러그매칭 파트너URL·cited_urls 확장, 96b6430). 사용자가 '위서클 이어가기', '출처 인용 지표', '실측 대시보드', '콘텐츠 밀도 루틴', 'A/B 슬러그 버그', '학습 신호 고도화', 'RLS 잠금', 'round138', 'geo260710' 같은 표현을 쓰거나 이 세션 컨텍스트를 복원할 때 사용. 이전 맥락은 geo-snapshot-260709 참조(누적).
---

# 위서클 GEO/AEO SaaS — 작업 스냅샷 (2026-07-10, Round 138)

> 이전 누적(Round ~137)은 **geo-snapshot-260709**. 본 문서는 이번 PMF 세션 추가분.
> 콘솔 = medimap-blog-v2 (geo-v2-beta.vercel.app) · 콘텐츠 채널 = medimap-blog (wecircle.co.kr) · DB = Supabase blogkey(gifopyowyankfsfghhdi) · 발행 = GitHub Actions(Python, Vercel 아님).

## 이번 세션 한 줄 요약
"AI가 우리 **이름**을 부른다(299회)"에서 **"AI가 우리 **콘텐츠를 근거(출처)**로 인용한다(현재 0→목표)"**로 PMF 성공 정의를 데이터로 재정립하고, 데모를 0으로 정리한 뒤 그 숫자를 올리는 루프(밀도 루틴·학습·A/B)를 전부 정합화.

## Round 138-A — 콘솔 홈: 데모→실측 대시보드 (커밋 1d0cc41, 라이브 검증)
- 🔴 발견: 콘솔 홈 `/`(page.tsx) + Sidebar 전체가 **Figma 데모(mock-data.ts) 가짜 KPI**. 클라이언트 로그인 시 보는 화면이 곧 이 가짜.
- **RPC `dashboard_overview(p_tenant)`** 신규(apply_migration): mentions/responses/queries/keywords 실집계 → visibilityKpis·mentionKpis·enginePerformance·keywordOptimization·sentimentByEngine·mentionTrend·overallSentiment·liveFeed·topicRows·aiInsightBanner jsonb. openai→chatgpt 매핑, stub 엔진 제외.
- **`/api/dashboard/overview?tenant=4`**(신규 파일) → RPC 호출. 기본 tenant 4(BGN).
- **page.tsx** = mock 제거, API fetch해서 기존 Figma 위젯 재활용(숫자만 진짜). 🔴 페이지가 RPC 결과를 그대로 렌더 → 이후 RPC만 고치면 프론트 배포·EF 없이 지표 추가 가능(핵심 트릭).
- 실측(BGN): 총 인용 299 · gemini205/claude61/chatgpt33 · 잠실라식50 · 최근7일103 · 긍정22/중립277.
- Chrome MCP로 `/api/dashboard/overview` JSON + `/` 렌더 검증(web_fetch는 클라이언트렌더라 로딩만 보임 → Chrome navigate+get_page_text로 확정).

## Round 138-A(측정) — 출처 인용 북극성 지표 (RPC 재적용, 즉시 라이브)
- 🔴🔴 세션 최대 발견: responses 2094건 중 cited_urls 있는 1548건인데 **우리 도메인(wecircle/medimap) 출처 인용 = 3~4건(사실상 0)**. AI는 브랜드를 **이름**으로 부르지 우리 **URL을 출처**로 안 씀.
- `dashboard_overview`에 `sourceCitation{ours,withSources,total}` + visibilityKpis 재편(브랜드 언급 vs **출처 인용(우리 콘텐츠)**). tenant4 = 출처인용 0 / 전체출처 359건 중.
- 냉정한 뉘앙스: **bgneye.com(BGN 자사 홈)은 출처로 72회 인용**됨. 즉 "출처 0"은 정확히는 "우리(wecircle) 콘텐츠가 0". 클라이언트 자사 사이트는 이미 인용됨. 경쟁 출처: modoodoc110·bnviit(비앤빛)101·youtube77·namu.wiki71·sueye61·topeye59·서울비전50·서울대병원42…
- **PMF 미션 확정: wecircle 콘텐츠를 이 출처 목록에 진입시키는 것.** 이 지표(0→목표)가 북극성.

## Round 138-B — 데모 클린업 Phase 1 (커밋 ee2df1e, 라이브 404 검증)
- 삭제: 데모 6페이지(`/blog /faq /simulator /video /data-feeding /ai-code`) + 스텁 5라우트(`api/blog/generate api/faq/generate api/simulator/compare api/publish/auto api/monitor/tick`). `site-config.ts` navGroups → 대시보드 링크만.
- 🔴 `/api/publish/auto`·`/api/blog/generate` 등은 **데모 스텁**(실발행 아님). 진짜 발행 = GitHub Actions `auto-publish.yml`(Python) → generated_contents insert(85편 발행됨).
- 🟢 남긴 것(실제): `/admin/*`(실 Supabase), `/scanner`(영업 깔때기), wecircle.co.kr 콘텐츠.
- 미완(Phase 1b): 내부 mock 잔재 — cost·funnel(admin) + llms.txt·llms-full.txt·sitemap.xml 이 아직 mock-data.ts 사용. mock-data.ts는 그래서 유지(빌드 그린).

## Round 138-C — 콘텐츠 밀도 루틴 (스케줄 + 첫 배치)
- **스케줄드 태스크 `wecircle-content-density`** (cron `0 9 * * 1,3` = 월·수, 회당 5개 = **주10개**). 프롬프트 자체완결(Supabase 프로젝트id·이기는토픽 SQL·비교표+FAQPage 템플릿·의료법·draft 적재). 파일: `C:\Users\user\Documents\Claude\Scheduled\wecircle-content-density\SKILL.md`.
- 로직: mentions 실인용 top 토픽 + 롱테일 변주 → generated_contents에 **draft** 적재 → 어드민 content-queue 검수·발행(의료법이라 무검수 자동발행 금지).
- 🔴 발견: 최근 발행이 "스마일" 한 키워드에만 몰려 있고 AI 인용 상위(잠실라식50·백내장38·노안교정35·라섹34)는 **콘텐츠 공백**.
- 첫 배치 수동 실행: 그 공백 5토픽 초안 **draft id 198~202** 적재(비교표+FAQPage raw_qa_pairs 4문항+의료법 준수). 🔴 함정: `blog_category` CHECK 제약(ai_trend/content_marketing/hospital_marketing만) → 파트너 의료글은 `blog_category=null`, `partner_category='eyeclinic'`.

## Round 138-D — RLS 잠금 (즉시 반영)
- anon 노출 6개 테이블 ENABLE RLS: content_settings, learned_insights, domain_classifications, tenant_domain_competition, applied_insights, ab_tests. 앱은 service_role(서버라우트)·SECURITY DEFINER RPC로 접근 → 무손상.

## Round 138-E — 경쟁사 SOV 진단 (경로2, 별개)
- 🔴 mentions 858건 전부 is_target, **is_competitor=0**. 진짜 SOV/인용순위 불가.
- 원인: 파서(`src/parser/mentions.py`)는 정상(competitors 리스트 받으면 태깅). `competitors` 테이블 0행 → 수집기(`collect.py`)가 빈 리스트 전달. **코드 아니라 데이터(경쟁사 브랜드 정의) 문제.**
- 사용자 방향 전환: 경쟁사 명단 대신, 이미 있는 **학습→반영→A/B 루프를 고도화**(아래 B·C).

## Round 138-F — 학습/AB 파이프라인 정합 (커밋 96b6430, 다음 GH Actions 검증)
- 🔴 진단: 학습 루프는 **작동 중**(learned_insights.applied=true 안과 8건이 domain_category 매칭으로 생성 프롬프트 주입). `applied_insights` 0행은 split-brain **죽은 레거시 테이블**(loader는 learned_insights.applied를 읽음). 파서 고장 아님.
- 🔴 A/B(ab_tests 3건) 카운트 전부 0 = 구조적. `analyze_competitor_citations`(learned_pattern.py)는 이미 진료과별 경쟁사 출처 인용 학습 중(좋음).
- **C 수정 (`src/content/learned_pattern.py` analyze_patterns):** 성공 신호를 브랜드 언급(proxy) → **출처 인용 ×10 + 언급 폴백** 결합(`kw_source` 쿼리 추가). 출처 인용 생기면 그 키워드 구조 우선 학습, 아직 0이면 언급으로 무회귀 폴백.
- **B 수정 (`scripts/run_ab_analysis.py` _count_citations):** 실버그 — `%/blog/{slug}%`만 매칭해 **파트너 콘텐츠(`/with-partners/.../{slug}`) 인용을 영원히 0으로 놓침** + cited_urls 무시. → 경로 무관 `%{slug}%` + source_domains·cited_urls 양쪽 매칭.
- py_compile 게이트: run_ab_analysis 직접 PASS. learned_pattern은 마운트 절단(462줄) → /tmp 재구성(마운트 head + 원본 tail, anchor assert) py_compile PASS.
- 🔴 검증: 둘 다 GitHub Actions 크론(`auto-pattern-learning.yml`=C, `ab-analysis.yml`=B) 다음 실행에서만 효과. Vercel 배포 무관. workflow_dispatch 수동 실행 가능.

## 함정 메모 (이번 세션)
- **RPC-우선 지표 추가**: 대시보드 페이지가 RPC jsonb를 그대로 렌더 → 지표는 RPC(apply_migration)만 고치면 프론트 배포·함정 EF 없이 즉시 라이브. (출처 인용 지표가 이 방식.)
- **마운트 절단(함정 ED) 재발**: 대형 .py/.tsx 편집 후 py_compile/esbuild가 tail 절단으로 false-fail → /tmp 재구성(anchor assert) 후 게이트. Edit 성공 = 호스트 무결.
- **함정 EF**: 이번엔 page.tsx·site-config·learned_pattern·run_ab_analysis 전부 git status에 정상 포착(M) — EF 안 터짐. 그래도 push는 항상 사용자 로컬, `git update-index --no-assume-unchanged` 폴백 준비.
- Chrome MCP가 클라이언트렌더 페이지/JSON 검증의 정답(web_fetch는 JSON 본문 안 보임).

## 라이브 URL (2026-07-10)
| 대상 | URL | 비고 |
|---|---|---|
| 콘솔(클라이언트) | https://geo-v2-beta.vercel.app | 홈 `/` = **실측 AI 인용 대시보드**(출처 인용 지표 포함) |
| 영업 앞문 | https://geo.wecircle.co.kr → /scanner | 리드 게이트 모달 |
| 콘텐츠 채널 | https://wecircle.co.kr | GEO 자산(with-partners) |

## 다음 라운드 후보
- **경로2 실행**: competitors 테이블 채워 진짜 SOV — 단 사용자는 "학습 루프 고도화"로 대체 원함. 우선순위 재확인.
- **B·C 검증**: workflow_dispatch 실행 후 ab_tests 카운트·learned_insights DB 실측.
- **첫 배치 5 draft(198~202) 검수·발행** → wecircle 콘텐츠 출처 인용 0→1 시도(북극성 실측 시작).
- **Phase 1b**: cost·funnel·llms·sitemap mock 제거.
- **고객 이메일 리포트**: Resend wecircle.co.kr Verified 대기(아직 Not Started).

## Round 138-G — 5글 발행 + 콘텐츠 시각 품질 (2026-07-11)
- 첫 배치 5 draft(198~202) 검수·발행: 의료법 금지어 게이트(정규식) 통과 → status='published', published_at=now(). URL = wecircle.co.kr/with-partners/eyeclinic/bgn/{영문slug}. ISR revalidate=60 → rebuild 없이 자동 렌더(getPartnerPost 필터 = status='published' AND compliance_status='pass'). cover_image null 우아 처리(조건부 렌더), 5글 상호링크.
- 🔴 콘텐츠 시각 품질 근본 버그: with-partners [slug] page가 `prose prose-stone`(Tailwind typography **미설치**, tailwind config plugins:[]) 사용 → h2·table 스타일 0(밋밋한 벽 텍스트). 반면 /blog는 `.prose-medimap`(globals.css 커스텀 — h2/h3/p/ul/ol/**table th·td 테두리**까지) 사용해 정상. → **수정: with-partners [slug] page className `prose prose-stone …` → `.prose-medimap max-w-none`(한 줄).** 모든 파트너 글(기존+미래) 즉시 개선. Chrome 스크린샷으로 표·제목·TL;DR 렌더 실측 검증 완료.
- 5글 콘텐츠 업그레이드: 🎯3줄 요약 TL;DR 박스(상단) + ❓자주 묻는 질문 본문 섹션(raw_qa_pairs 노출) DB body 추가.
- 루틴 시각 표준화: scheduled task `wecircle-content-density` 프롬프트에 시각 품질 표준 주입 — TL;DR 박스·이모지 H2 5개+·비교표·본문 FAQ(h2/h3)·짧은 단락(4줄)·정의형 문장. 다음 월·수부터 이 품질.
- ② 학습루프 완결(커밋 별도): `learned_pattern.upsert_auto_pattern_insight` = dedup(DELETE 이전 auto_pattern) + applied=**true** 등록. `applied_insights_loader` = `or=(domain_category.eq.{cat},domain_category.is.null)` → 전역 자동패턴도 생성 프롬프트에 주입. (구멍: 자동패턴이 domain_category=null이라 로더가 안 읽던 것 해소.)
- 🔴 함정: web_fetch는 본문을 마크다운 변환 → CSS 스타일·ISR 최신본 검증 불가. **시각 검증 = Chrome 스크린샷 필수.** ISR stale-while-revalidate라 첫 요청 stale·재요청 fresh. 파트너 글 URL 경로에 대괄호 → git add는 디렉토리 단위(글롭 충돌 회피).

## 실측 KPI (2026-07-11 기준)
- BGN(tenant4): 브랜드 언급 299 · **출처 인용(우리 콘텐츠) 0** / 전체 출처 359건 중 · 대시보드 라이브.
- 발행 콘텐츠: generated_contents published 90편(신규 5 포함). 안과 경쟁 출처 = modoodoc·bnviit·namu.wiki·bgneye(자사홈 72)…
- A/B 3건 running·카운트 0(출처인용 0 현실). learned_insights 자동패턴 최신 applied=true.

## Round 138-H — Resend 고객메일 + 추이차트 1000행 버그 근본수정 (2026-07-11 오후)
- **Resend wecircle.co.kr Verified** 완료(DNS·도메인 Jul11 12:09~12:11). → 임의 고객 주소 발송 가능.
- **고객 리포트 이메일:** `/api/scanner/lead` 에 `sendCustomerReport(b)` 추가 — 폼 제출 고객에게 점수·등급·의료법 상태 + 카카오 상담 CTA 브랜드 메일(best-effort). 🔴 켜려면 Vercel geo-v2 env `RESEND_FROM='WECIRCLE GEO <alerts@wecircle.co.kr>'` + Redeploy(사용자). 미설정이면 onboarding@resend.dev로 남아 고객 도달 X(무해).
- 🔴🔴🔴 **추이 분석 차트 "07-04부터 안 보임" = PostgREST 기본 max-rows 1000행 한도(진짜 원인).** 30일 queries/responses 수천 건인데 단일 `.select()`가 오래된 1000건(≈06-11~07-02)만 받아 최근이 통째로 잘림. **측정·데이터는 07-10까지 멀쩡**(경쟁사 인용 272~478/일, BGN bgneye 2/일). 측정 실패도 A도 아니고 **행 개수 한도**였음.
  - 전수 조사(python 감사): 큰 테이블(responses·queries·mentions·llm_call_logs) unbounded select ~15개 API/페이지 — citations·citations/keyword·competitors·competitors/trends·saas-tracking·partner-leaderboard·citation-paths·domain-context·domain-history·auto-classify-domains·cron/citation-alerts·funnel·cost·admin/(portal)/page.tsx·reports. 전부 동일 잠재 버그.
  - **✅ 근본 수정(파일 0곳):** `alter role {anon,authenticated,authenticator,service_role} set pgrst.db_max_rows='100000'; notify pgrst,'reload config';` → 55곳 한 번에 해결. (지속성 위해 Supabase 대시보드 Settings→API "Max Rows"=100000 권장 — config 재생성 시 덮일 수 있음.) `statement_timeout=8s`로 폭주 방지.
  - 보조 병행: trends route에 range 페이지네이션 코드 작성(미푸시, max-rows로 이미 해결돼 선택). **A** = 차트가 competitor_landscape(주2회)만 보던 걸 own(매일) 키워드 포함으로(route.ts kwQuery `.or(competitor_landscape,own,null)`). **C** = `measure-competitor-mentions.yml` cron `0 21 * * 1,4`(월·목) → `0 21 * * *`(매일).
- **claude 엔진 다운**: 크레딧 소진(07-05~ gemini/openai 2개만). 사용자 충전 예정 → 키 유효하면 다음 측정부터 3엔진 자동 복구(코드 변경 불필요).
- 🔴 함정(신규): **차트/집계 데이터가 특정 날짜부터 비면 → 측정 실패보다 PostgREST 1000행 한도를 먼저 의심.** 큰 테이블 N일 집계는 max-rows 상향 or range 페이지네이션 필수. web_fetch는 admin-gated·CSS 못 봐서 검증 불가 → 사용자 스크린샷/Chrome.

## Round 138-I — 어드민 긴 리스트 페이지네이션+검색 (UX 고도화, 2026-07-11)
- **재사용 컴포넌트 `src/components/admin/ListView.tsx`(신규):** `useListView(items, {size, search})` 훅 + `<PageBar lv={}>` + `<SearchBox lv={}>`. 기능 = 검색 필터 + 페이지네이션(이전/다음·"3/12") + 페이지당 개수(10/20/50/100) + "N개 중 X–Y" 카운트. 앞으로 긴 리스트는 3줄로 재사용.
- **적용 3페이지(fragment 없는 sibling 패턴 — SearchBox/PageBar를 카드 div의 형제로 삽입, `{cond && (<div>...</div>)}`):**
  - `learned-insights`: 인사이트 리스트 (도메인·키워드·진료과 검색, 20/페이지) → "26개 중 1–20·1/2" 검증.
  - `citations`: 키워드별 인용 표(15/페이지) + 경쟁사/플랫폼 도메인 표 — 기존 `.slice(0,15)` 하드컷 **제거** → 전량. max-rows 100k + 페이지네이션 = **도메인 1026개 탐색**("1026개 중 1–10·1/103").
  - `saas-tracking`: grounding 표(15/페이지) + 경쟁 SaaS 도메인 리스트(20/페이지) + 검색.
- content-queue는 기존 페이지네이션(25편/4p) 있어 미변경.
- 🔴 함정: 큰 .tsx 편집 후 esbuild가 **마운트 tail 절단(함정 ED)**으로 false-fail → `tr -d '\000'` + 호스트 tail(Read 도구) 재접합해 `/tmp` esbuild로 게이트. sibling 삽입은 카드/섹션 div가 다자식 허용이라 **fragment 불필요**(삼항 브랜치 안엔 넣지 말 것).

## 다음 세션 시작 시 체크
- Vercel geo-v2 env `RESEND_FROM` 설정됐는지(고객메일 on) · Supabase Max Rows 대시보드 반영 · claude 크레딧 충전 여부 · C 매일 측정 반영 · 새 5글 출처 인용 0→N 이동(3~4일 후) · **`ListView` 재사용 컴포넌트 존재**(긴 리스트 페이지네이션).
