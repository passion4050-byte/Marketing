---
name: geo-snapshot-260713
description: 위서클(WECIRCLE·구 메디맵) GEO/AEO SaaS 작업 스냅샷 — 2026-07-13. ①어드민 언어 스코프 전면화(대시보드 전 패널·citations·competitors) + 리더보드 fetch race 버그 수정 ②측정데이터·AEO 3대 기능(리포트 실측지표·A/B AEO 점수·학습 자동화) + Princeton GEO 기반 AEO 점수 lib ③해외 병원명 다국어+MedicalClinic 스키마 ④git gc 루프 회피 ⑤[후반 노트북세션] auto-learn 내부호출 헤더버그 수정·생성프롬프트 통계필수 디렉티브(AEO+30~41%)·해외 en/ja/zh about/contact 미러+홈 패리티+sitemap. 사용자가 '위서클 이어가기', '언어 스코프', '리더보드 race', 'AEO 점수', 'scoreAeo', '리포트 지표', 'A/B AEO', 'auto-learn', '학습 자동화', 'gc 루프', '통계 디렉티브', '해외 about contact', 'UI 미러', 'geo260713', 'round141' 같은 표현을 쓰거나 이 세션 컨텍스트를 복원할 때 사용. 이전 누적은 geo-snapshot-260712 참조.
---

# 위서클 GEO/AEO SaaS — 작업 스냅샷 (2026-07-13, Round 141)

> 이전 누적은 **geo-snapshot-260712**. 본 문서 = 언어 스코프 전면화 + 측정데이터·AEO 기능개발 세션.
> 콘솔/어드민 = medimap-blog-v2 (geo-v2-beta.vercel.app, git 자동배포) · 콘텐츠 채널 = medimap-blog (wecircle.co.kr) · DB = Supabase gifopyowyankfsfghhdi · Vercel 팀 `medimaps-projects`(Hobby).

## 🔴🔴 세션 최대 교훈 3가지
1. **OneDrive 안의 `.git` = gc 무한 프롬프트 루프.** 커밋 시 git auto-gc가 `.git/objects/00` 삭제를 시도 → OneDrive 잠금과 충돌 → "Deletion of directory failed. Should I try again?(y/n)"가 256개 폴더마다 무한 반복. `gc.auto 0`으로도 완전히 안 막힘. **확실한 회피 = OneDrive 트레이 → 동기화 일시중지(2h) 후 push.** 근본해결 = repo를 OneDrive 밖(예: `C:\dev\Marketing`)으로 이동. 탈출 = Ctrl+C 또는 창 닫기(커밋 오브젝트는 이미 기록됨, 무손상).
2. **client fetch race — scoped 컴포넌트 필수 가드.** 초기 props 없는 client 컴포넌트(리더보드)는 mount 시 scope='all'로 먼저 fetch(느린 JS 라우트) → setScope(readScope())로 scoped fetch(빠른 RPC). **취소 가드 없으면 느린 all 응답이 빠른 scoped 응답을 덮어씀** → all 데이터 표시 버그. 해결 = useEffect cleanup `cancelled` 플래그 or `reqSeq` ref로 stale 응답 무시. (citations/competitors 페이지도 seq 가드 적용.)
3. **workflow 파일 push는 `workflow` OAuth scope 필요.** git 토큰에 workflow scope 없으면 `.github/workflows/*.yml` push가 `! [remote rejected] ... refusing to allow an OAuth App to create or update workflow`로 거부됨. 회피 = 워크플로만 빼고 push(`git reset --soft HEAD~1; git restore --staged <yml>`) 후, 워크플로는 **GitHub 웹 Add file로 추가**(웹 커밋은 scope 불필요).

## Round 141 — 언어 스코프 전면화 (스샷 검증 완료)
### 아키텍처 (정본)
- **스코프 값 이원화(중요):** 측정 keywords.lang=`zh-Hant`, 콘텐츠 generated_contents.lang=`zh-Hans`. → 대상 테이블에 맞는 매퍼 필수: `scopeToKeywordLang`(zh→zh-Hant) / `scopeToContentLang`(zh→zh-Hans). `src/lib/scope.ts`(서버·쿠키), `src/components/admin/ScopeSelector.tsx`(client·localStorage+이벤트+쿠키+router.refresh).
- **SSR 패널(대시보드 page.tsx 1276줄):** ScopeSelector가 쿠키(`wc_admin_scope`)+router.refresh → `getScopeServer()` 서버 읽기. `langKwIds`(해당 lang keyword_id 집합) 1회 계산 → 모든 `queries` fetch에 `.in('keyword_id', langKwIds)` 연쇄 필터. mention은 keyword_id 없어 respRows(query_id)로 in-memory 필터.
- **client 패널:** readScope()+SCOPE_EVENT 구독 → API에 `?scope=` → 라우트가 langKwIds로 필터. **all=필터 없음=무회귀**(통합/국내 원본 유지).
### 신규 RPC (전부 라이브 검증)
- `partner_leaderboard(_days,_kw_lang,_content_lang)` — 리더보드. 통합=817, 국내=815, EN=2(청담디어), ZH=0 실측 일치.
- `citation_count(_hours,_kw_lang)` — KPI 인용카드. 24h/30d.
### 적용·검증(스샷)
- **대시보드:** 리더보드·KPI·성과분석 차트(tier/client ranking/keyword grounding/newDomains)·Top콘텐츠·구조·운영로그 전부 스코프. EN/JA/ZH=해당언어만, 통합 무회귀. **🔴 리더보드 race 버그 발견·수정(cancelled 가드).**
- **citations 페이지:** 통합 812/5158 → EN·ZH 0 (라우트 ownKwQuery+queriesQuery lang필터, mention trend respRows 필터). seq 가드.
- **competitors 페이지:** 통합 경쟁도메인 → ZH 빈상태. landscapeKwQuery+queriesQuery lang필터.
- **스코프 불필요:** SaaS노출도=is_saas_marketing ko 10개뿐(자사 SaaS 한국시장). reports·학습·A/B·cost·문의=측정데이터 미사용→기능개발로 전환.

## Round 141 — 측정데이터·AEO 기능 (딥리서치 기반)
### AEO 점수 lib `src/lib/aeoScore.ts` (핵심 공용)
- Princeton GEO(KDD2024)·Ahrefs 17M citations 딥리서치 근거. `scoreAeo({body,faqCount,publishedAt,hasFaqSchema,hasMedicalSchema})` → {score,grade,items}.
- **9개 가중 항목:** direct_answer12·statistics15·quotes_cites15·structure15·tables10·faq10·eeat13·freshness5·schema5.
- 근거: 통계+30~41%·인용문+37%·출처+30%·표+47%·구조28~40%·최신성(AI트래픽 65% 1년내)·의료 저자/감수(YMYL).
- 검증: 좋은콘텐츠 93 A / 나쁜 18 D. 기존 `scan.ts`(URL 7항목)·`contentQuality.ts`(기본)와 구분되는 콘텐츠 body용.
### ① 리포트 (reports/email/route.ts)
- `computeReportMetrics(sb,tenantId)` — 30일 발행수·AI 인용수·평균 AEO 점수·최고 AEO 콘텐츠. 이메일 본문에 지표 블록. (기존=수치 없는 안내만.)
### ② A/B (ab-tests route+page)
- variant contents SELECT에 body/raw_qa_pairs/published_at 추가 → variant별 scoreAeo → `aeo`·`aeoGrade` 반환. 페이지에 AEO 배지(인용수=우선지표 유지). 라이브 확인: AEO 67·B vs 55·C.
### ③ 학습 자동화 (auto-learn/route.ts + 워크플로)
- responses.source_domains(is_self=false=경쟁사) 집계 → **PLATFORM_BLOCKLIST**(youtube/google/naver/namu/디렉토리/미디어 제외) → top N 경쟁 병원 → 기존 learn-from-domain(analyze+save) 내부 호출 → learned_insights 누적. CRON_SECRET 인증. 주1회 cron(`.github/workflows/auto-learn.yml`, 월 08:00 UTC).
- 검증: 엔드포인트 401(인증 작동)·CRON_SECRET 설정 확인·SQL로 경쟁병원 집계 실증(bnviit·bgneye·newhairps).

## Round 141 후반 — 오늘 노트북 세션 이어서 (3건 push 완료)
> repo 는 `C:\dev\Marketing`(OneDrive 밖) 로 이미 이동됨. gc 루프는 여전히 재발하나 **패턴 확립: 커밋/머지는 gc 프롬프트 전에 완료됨 → gc 뜨면 Ctrl+C → 별도 `git push` (push는 gc와 무관, 프롬프트 안 뜸).**

### ④ auto-learn 내부호출 헤더 버그 (핵심 — learned_insights 0의 진짜 원인)
- 증상: 워크플로 성공(200)인데 learned_insights 안 쌓임. 원인 = auto-learn/route.ts 가 **내부에서** `/api/admin/learn-from-domain` 을 self-call 하는데, 그 fetch 에 x-cron-secret 헤더가 없어 **미들웨어가 자기 내부호출을 차단**(admin 쿠키 없는 cron 컨텍스트).
- 수정: `internalHeaders = {Content-Type, ...(secret?{'X-Cron-Secret':secret}:{})}` 만들어 analyze/save 두 fetch 에 전달. (워크플로 yml 은 이미 최상위 curl 에 `-H "X-Cron-Secret"` 있음 — 이건 사용자가 GitHub 웹으로 추가했었음.)
- DB 실측 후보(30일, 블로클리스트+14일중복 필터 후): bnviit=제외(07-04 학습, 14일내), **bgneye·newhairps·seoulvisionclinic** 3곳이 topN=3 → 실행 시 learned 28→31 예상.
- 🔴 미검증: 워크플로 1회 Run 필요(월 17:00 KST 자동 or 수동). 실행되면 `SELECT count(*) FROM learned_insights` 로 확인.

### ⑤ 생성 프롬프트 통계 필수 디렉티브 (AEO 최대 지렛대)
- 감사: 해외 발행 34/34 글이 `%수치 통계` **0개**(구조·표·FAQ·최신성은 100%). Princeton 실증 통계=인용 +30~41% 단일 최대 요인인데 전면 결손.
- `src/content/generator.py`: `_STATS_ENFORCE_DIRECTIVE` 상수 추가(출처 명시 %통계 2개+ 강제, 🔴의료법 가드: 효과·성공률 단정/보장 금지, 임의수치 날조 금지) → `_combined_directive` 에 구조/변주와 함께 주입. py_compile PASS.
- 🔴 미검증: 라이브 발행 있어야 실효 확인 → 다음 발행분 AEO 재감사(%통계 0→N).
- ⚠️ ko 포함 전체 적용(해외만 게이트 안 함) — 콘텐츠 인용에 국내도 이득이나, A/B 베이스라인 영향 관찰 필요.

### ⑥ 해외 en/ja/zh UI 미러 (전환·브랜드 신뢰 — 사용자 명시 요청)
- 해외 `/en` = 환자용 아님, **한국 병원 대상 B2B 세일즈 랜딩**(디자인=SaaS #1B68FF, KR 매거진과 다름). → KR 구조·의도를 해외 SaaS 디자인 언어로 옮기고 언어별 트랜스크리에이션(KR 스타일 복붙 X).
- 신규: `/{en,ja,zh}/about`(4원칙 Editorial·Compliance·AI Citation·Measurement + 연혁 2024~2026 + 매니페스토 + Organization JSON-LD), `/{en,ja,zh}/contact`(전환 CTA + 다음단계 3스텝 + 사업자정보·이메일).
- 편집: ja/zh 홈에 EN에만 있던 shift 밴드 + 경쟁사 비교표 추가(3언어 동급). nav 에 About/Contact. sitemap 에 6 URL + 각 페이지 hreflang alternates.
- CTA = `ContactButtons`(WhatsApp/LINE) 컴포넌트가 `siteConfig.contact.whatsapp/.line`(=env `NEXT_PUBLIC_WHATSAPP`/`_LINE`) 읽음. **하드코딩 안 함 — 라이브에 `wa.me/`·`line.me/` 빈 placeholder 확인. 사용자가 env 넣으면 전 페이지 자동 반영.**
- 라이브 실측(web_fetch): /en(홈 nav+비교표)·/en/contact·/ja/about·/ja(홈 shift+비교표) 전부 정상 렌더 확인. (/en/about web_fetch 빈응답은 그 URL 캐시 아티팩트 — 동일 구조 타 페이지 정상이 증거.)
- 의도적 제외(스파링): `/guide`(병원 입점 온보딩=환자 무관), privacy/terms(실법무문구 필요 — 사용자 원문 제공 대기).

### 🔴 함정 재확인 — 마운트 잘림 (함정 ED)
- sitemap.ts 편집 직후 esbuild "Unexpected end of file" → 샌드박스 마운트가 126/143줄에서 잘려 보임. **호스트 Read 는 143줄 온전(무손상).** 게이트: 마운트 head-126 + 호스트 tail(Read 로 확보) 재조립 → /tmp esbuild PASS. **호스트 Edit/Write = 정본, 마운트 불일치 = 동기화 지연일 뿐.**

## 데이터 실상 (기억)
- 측정 데이터 lang: keywords ko114·en8·ja5·zh-Hant5 / generated_contents ko125·en12·zh-Hans11·ja11.
- **해외 인용 target=en2·ja0·zh0** — cron은 정상(어제 en16·ja10·zh10 쿼리), AI가 경쟁사(seoulvisionclinic 등)만 인용=진짜 낮은 점유율(콘텐츠 볼륨 문제). 엔진 2개(gemini·openai)만, claude·perplexity 크레딧 꺼짐.
- 해외 콘텐츠 파트너=청담디어(dear) 1곳(en/ja/zh 각4). 다국어 표시명: Cheongdam Dear Clinic / 清潭ディア医院 / 清潭Dear医院.

## 라이브 URL / 배포
- 콘솔·어드민 https://geo-v2-beta.vercel.app · 콘텐츠 https://wecircle.co.kr · 스캐너 https://geo.wecircle.co.kr/scanner
- 오늘 배포 커밋(전부 Ready): 90239c5·5e36fb0(스코프 기반) d0e5024(guides 다국어) 62b1214·41ad591(MedicalClinic) 0a263dd·14f37b0(대시보드 스코프+KPI) d73e8ea(리더보드 race) 2f5a9d2(competitors) d37f155(citations) fb14cc4(AEO lib) 057b118(리포트) ca6bf25(A/B) 742c86d·b482d73(auto-learn) 2275f75(워크플로).

## 다음 라운드 (미완) — 사용자 액션 대기 2건 + 개발 후보
### 🔴 트리거 대기 (사용자/시간, 그다음 내가 실측)
1. **auto-learn 워크플로 1회 Run** (GitHub Actions→auto-learn→Run, 또는 월 17:00 KST 자동) → `SELECT count(*) FROM learned_insights` 28→31 확인.
2. **통계 디렉티브 실효** — 다음 콘텐츠 발행분 나오면 해외글 AEO %통계 재감사(0→N).
3. **CTA 실번호** — 사용자가 Vercel env `NEXT_PUBLIC_WHATSAPP`·`NEXT_PUBLIC_LINE`(선택 `_PHONE`) 넣으면 about/contact/홈/헤더/푸터 자동 반영 → 재확인.
4. **해외 privacy/terms 3언어** — 사용자 원문 제공 시 넣기.
### 개발 후보
- **시술 허브 리스티클**(레퍼런스 seoulorthopedics·himedi·jivaka·seoulgynecology "Top N clinics+FAQ+구조화" AEO 아키타입) — 프론트 archetype(`/{lang}/guides/[slug]`)은 이미 있음, **콘텐츠 볼륨**이 병목.
- 해외 측정 실데이터 = claude·perplexity 크레딧 켜기 + 콘텐츠 볼륨.
- reports 화면 페이지(/admin/reports/[tenantId])에도 AEO 섹션 추가 여지.
- 통계 디렉티브 ko A/B 베이스라인 영향 관찰(필요시 해외만 게이트).

## 오늘 세션 push 커밋 (C:\dev\Marketing, main)
- `3f7abc6` auto-learn 내부호출 x-cron-secret 헤더(merge 경유) · `7ef1281` generator.py 통계 디렉티브 · `d733b39` 해외 about/contact+홈 패리티+nav+sitemap. 전부 origin/main Ready.
