---
name: geo-snapshot-260817
description: 위서클(WECIRCLE) GEO/AEO SaaS 스냅샷 — 2026-08-16~17 Round 161~164b. 해외 자동발행 전환·growly 2축 분석·지도 축 실장(NAP 카드/리뷰 퍼널/Places API 리뷰 동기화/Reddit 모니터)·citations RPC·1,000행 캡 전수 수술·봇 오염 정정(크롤러 지표 자산화)·전 병원 4개 언어 키워드 시딩·타깃 경로 LANG_ONLY 버그 수정. '위서클 이어가기', 'geo260817' 트리거. 정본 = Marketing 루트 SKILL.md Round 161~164b.
---

# 위서클 GEO 스냅샷 2026-08-17 (Round 161~164b)

## 세션 시작 규약
1. 사용자에게 git pull 안내 (Marketing = 모노리포, git 루트 = Marketing).
2. Marketing 루트 SKILL.md 의 Round 161~164b 섹션이 정본 — 이 파일은 진입용 요약.
3. SKILL.md append 는 반드시 Marketing 루트에 (medimap-blog-v2/SKILL.md 실사고 재발 금지).
4. 프로젝트 메모리: MEMORY.md → round162-map-axis.md, round160-multilang-routine.md, cloud-session-toolchain.md.

## 다음 세션 첫 작업 (검증 대기)
- Round 164b 푸시 + 재실행 2개(Daily brighteye all langs / Seed multilang keywords) 결과:
  ① tenant 19 — 5개 언어 각 1편 published ② 전 병원 4개 언어 keywords 시딩
  ③ crawler_hits 적재 → /admin/traffic 크롤러 위젯 ④ Reddit 모니터 0건 지속 시 OAuth 전환
  ⑤ OpenAI·Gemini 측정 429 quota 복구 (Claude만 성공 중 — 커버리지 1/3).
- 판정 지표: ~8/30 GSC 주간 클릭 추이 (brighteye 3개월 테스트).

## 이번 세션 핵심 구조물 (라이브)
- 지도 축: tenants NAP 5필드(+google_rating/count/place_id) → ClinicNAP 카드(리뷰 인용 자동 점등,
  brighteye ★5.0/2,164 실측) · /review/{slug} 퍼널(봇 무기록) · map-axis-monitor.yml(3일).
- 다국어 머신: 전 테넌트 auto_publish ON(린터 pass 게이트) · tenant_products 전 병원 4개 언어 ·
  seed_multilang_keywords.py(멱등) · daily-brighteye-all-langs · scheduler 타깃 경로 lang/market 필터.
- 지표 무결성: fetchAllRows/fetchByIdChunks 규약 · citations_dashboard RPC(jsonb 단일값·272ms) ·
  shortlink_clicks.is_bot · 기록 시점 봇 차단 · crawler_hits(+middleware /en~/tw·/review 확장).

## 재발 방지 교훈
- supabase-js 서버 캡 1,000행: .limit(20000)도 잘림 → fetchAllRows / RPC jsonb.
- 클릭·전환 지표는 봇 필터 없이는 지표가 아니다 (82% 오염 실사고 — GPTBot79·ClaudeBot30).
- 타깃/우선 분기는 새 필터 파라미터의 사각지대 — 필터 추가 시 전 분기 grep.
- GH Actions vars 미정의 = 빈 문자열 → python os.getenv 기본값 무시. || 폴백 필수.
- 신규 cron 첫 스케줄 미발화 잦음 — 수동 1회 실행 검증.
- 카카오 정본 https://pf.kakao.com/_xouLiX/chat (_xnWQkG 금지) · GSC = URL 접두어 속성.

## 사용자 결정 기록
- Reddit 인위 참여 거부 유지(모니터+공개 신분 대체, 수용) · 리뷰 대리 작성 거부(요청 퍼널만) ·
  병원 동의 범위 = 리뷰 가져오기까지(QR 보류) · wecircle 자체 GBP 정정(지도 축은 장소 단위) ·
  AI 크롤러 봇 트래픽 = 마케팅 지표로 자산화(사용자 지시).
